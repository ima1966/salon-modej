# サロン Mode J 売上管理システム ユーザーマニュアル

## 📋 更新履歴 (Version History)

| バージョン | 日付 | 更新内容 |
| :--- | :--- | :--- |
| **v14.6.01** | 2026/02/17 | ・アプリ全体の配色ルール統一（土曜＝青、水・日＝赤の背景色＆文字色）<br>・売上管理リストの視認性改善 |
| **v14.6** | 2026/02/17 | ・売上一覧の色分けルール変更（祝日の赤色表示を廃止し、定休日である水・日のみを赤色表示） |
| **v14.5** | 2026/02/17 | ・売上一覧の視認性向上（グラフ・表の色分け：水/日＝赤、土＝青）<br>・一覧表への「曜日」列追加 |
| **v14.4** | 2026/02/15 | ・スマホ「ダッシュボード」決済分析テーブルの横スクロール対応<br>・キャッシュ対策のためのファイルバージョン更新 |
| **v14.3** | 2026/02/15 | ・Webデプロイ時のパス設定修正（ルートindex.htmlの参照先是正） |
| **v14.2** | 2026/02/15 | ・Android端末での日付表示不具合修正<br>・Google Apps Script (GAS) 連携機能の復旧 |
| **v14.1** | 2026/02/11 | ・システム全体のリファクタリング<br>・UIデザインの刷新（モダンな配色とレイアウト） |

## 1. アプリの概要
本アプリは、サロンの日々の売上を簡単な操作で登録し、自動で集計・分析を行うためのツールです。
登録されたデータは自動的にクラウド（Googleスプレッドシート）に保存され、日別・月別の売上推移やトレンドをリアルタイムで確認できます。

---

## 2. 画面構成と主な機能

画面下部のメニューボタンで、3つの画面（タブ）を切り替えて使用します。

### ① 📝 売上入力（メイン画面）
日々の会計時にお客様の売上を入力する画面です。

**操作手順:**
1.  **日付**: 当日が自動入力されています。必要なら変更します。
2.  **お客様名**: 「山田花子」のように入力します（※カンマ `,` は入力不可）。
3.  **商品・施術明細**:
    *   **カテゴリー**: 「施術」「サブスク」「ベルマン」などをリストから選択。
    *   **商品名**: 具体的なメニュー名や商品名を入力します。
    *   **個数・単価**: 数字を入力すると、小計が自動計算されます。
    *   **店長担当**: 店長が担当した場合は「👑」マークにチェックを入れます。
    *   **明細の追加**: 複数の商品がある場合は「＋ 明細を追加」ボタンを押します。
4.  **決済方法**: 「現金」「振込」「カード」のいずれかをタップして選択します。
5.  **登録**: 「💾 売上を登録」ボタンを押すと完了メッセージが表示され、データが保存されます。

**ポイント:**
*   画面を切り替えると、入力内容は自動的にリセット（クリア）されます。

---

### ② 📊 売上一覧（分析・編集）
過去の売上データを確認、編集、分析する画面です。

**主な機能:**
*   **フィルタ機能（上部ボタン）**:
    *   「本日」「今週」「今月」などのボタンで、表示する期間をワンタッチで切り替えられます。
    *   **期間指定**: カレンダーで日付範囲を指定し「適用」を押すと、その期間のデータのみ表示します。「クリア」で今月の表示に戻ります。
*   **グラフ表示**:
    *   **年間推移**: 月ごとの売上合計を棒グラフで表示します。棒グラフをクリックすると、その月の詳細が下の日次グラフに表示されます。
    *   **日次推移**: 1日ごとの売上の波を確認できます。
*   **売上リスト（下部）**:
    *   売上データが新しい順にリスト表示されます。
    *   **編集**: 「編集」ボタンを押すと、その場（一覧画面内）で内容を修正できます。修正後は「変更を保存」を押してください。
    *   **削除**: ゴミ箱アイコンを押すと、そのデータを削除できます（※慎重に操作してください）。
*   **CSV出力**: 表示されているデータをExcelなどで開けるCSV形式でダウンロードします。

---

### ③ ⚙️ ダッシュボード・設定
経営状況の全体像を把握するための画面です。

**ダッシュボード:**
*   **主要指標**: 「本日の売上」「今月の売上」「平均客単価」などがひと目で分かります。
*   **日別トレンド表**:
    *   日付ごとに「売上」「客数」「件数」「前日比」「7日平均」「30日平均」を表形式で確認できます。
    *   項目名（売上など）をクリックすると、並び替え（ソート）が可能です。
    *   **前日比**: 前日に比べて売上が上がったか（緑▲）下がったか（赤▼）を表示します。
    *   **移動平均**: 「7日平均（短期的トレンド）」と「30日平均（長期的トレンド）」を表示し、日々の変動に惑わされずに実力を把握できます。
    *   **👑 店長担当実績（v14.1追加機能）**:
        *   **店長担当売上**: 明細入力時に「店長担当（👑）」にチェックを入れた分の売上を集計します。
        *   **店長担当顧客数**: 店長が担当したユニーク顧客数を表示します。
        *   **カテゴリー別**: 店長担当分の売上をカテゴリーごとに内訳表示します。

### ④ ⚙️ 設定（システム設定）
アプリの機能設定やデータ管理を行う画面です。上から順に各項目の意味と機能をご説明します。

**📊 Google Sheets 連携**
このアプリと、データ保存先であるGoogleスプレッドシートとの接続に関する設定です。
*   **GAS URL**:
    *   現在接続されているクラウド側プログラム（Google Apps Script）のアドレスです。
    *   *※通常は変更する必要はありません。*
*   **連携モード**:
    *   現在の通信方式を表示しています（例: 「CORS対応版 (no-cors)」など）。
*   **🔌 接続テスト**:
    *   ボタンを押すと、Googleスプレッドシートと正常に通信できるかを確認します。
    *   *※「保存できない」「動きがおかしい」と感じた時に、通信エラーが原因かどうかを切り分けるために使用します。*

**💾 データ管理**
アプリ内（お使いのスマホやPC）に保存されているデータに関する操作です。
*   **保存済みデータ**:
    *   現在、この端末（ブラウザ）の中に保存されている売上データの件数を表示しています。
*   **⬇️ Google Sheetsから復元 (上書き)**:
    *   クラウド（Googleスプレッドシート）にある最新の全データをダウンロードし、この端末のデータをそれで上書きします。
    *   *※機種変更した時や、他のスタッフが入力した最新データを自分の端末にも反映させたい時に使用します。*
    *   *※【注意】この端末で入力中かつ未送信のデータがある場合、それらは消えてしまうのでご注意ください。*
*   **⬆️ Google Sheetsへインポート**:
    *   この端末にある全データを、強制的にGoogleスプレッドシートへ送信（保存）します。
    *   *※通常は売上入力時に自動保存されるため使いませんが、通信エラー等で万が一保存されていなかった場合の「再送信」として使用します。*
*   **🗑️ Local削除**:
    *   この端末（ブラウザ）内のデータを全て消去します。クラウド上のデータは消えません。
    *   *※アプリの動作が非常に重くなった場合や、端末内のゴミデータを掃除したい場合に使用します。*
*   **🔥 Cloud削除**:
    *   **【危険】** Googleスプレッドシート上の全データを完全に削除します。
    *   *※テスト運用後にデータをリセットしたい場合など、特殊な状況以外では絶対に使用しないでください。*

**ℹ️ システム情報**
アプリ自体の情報です。
*   **バージョン**:
    *   現在のアプリのバージョン番号です（例: v14.1）。
*   **最終更新**:
    *   このアプリのプログラムが最後に更新された日付です。


---

## 3. 注意事項
*   **インターネット接続**: データの保存にはインターネット接続が必要です。オフラインでも入力はできますが、ネット復帰後に反映させる必要があります。
*   **ブラウザ更新**: 動作がおかしいと感じたら、ブラウザの更新ボタン（リロード）を押してください。

---
作成日: 2026/02/15
バージョン: v14.4
